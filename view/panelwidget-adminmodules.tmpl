<ul class="naviList">
{foreach $modules module}
	<li class="{$module.key} {if $active == $module.key}active{/if}">
		<a href="?ext=admin&amp;mod={$module.key}">{$module.label}</a>
	</li>
{/foreach}
</ul>